# 统一按 LF 入库。
#
# 背景：本机 `core.autocrlf=true`，Windows 工作区里 36 个 *.go 文件是 CRLF、45 个是 LF。
# 这种混杂会让 `gofmt -l` 报出几十个「假失败」，也会让 prettier 的本地结果与 CI 不一致。
# 声明 `text eol=lf` 后，检出与提交都按 LF 归一化，工作区不再因行尾产生噪声 diff。
# 详见 docs/stage5/baseline.md 第 9.1 节 B2 与 docs/stage5/decisions.md 第 7 节。
* text=auto eol=lf

# Windows 脚本必须保留 CRLF：PowerShell 5.1 对 LF 的 .ps1 容错较差。
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

# 二进制资源不做行尾转换。
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.mp4 binary
*.woff binary
*.woff2 binary
